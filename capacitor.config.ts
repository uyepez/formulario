import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apruebaformulario.mx',
  appName: 'formulario',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
